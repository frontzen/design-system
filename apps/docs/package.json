{
  "name": "docs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "yarn run -T vite",
    "build": "yarn run -T vite build",
    "test": "yarn run -T vitest --run",
    "type-check": "yarn run -T tsc"
  }
}
